<!DOCTYPE html>
<html class="no-js">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />

    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>
    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}" />
    {% endif %}
    {% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}" />
    {% endif %}

    {% if current_page %}
        <link rel="canonical" href="{{ current_page.url }}" />
    {% endif %}

	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
	
</head>
<body{% if config.theme_config.widescreen %} class="widescreen"{% endif %}>

    <div id="header" role="banner" class="container">
        <div class="row">
            <!--Title & Tagline-->
            <div id="title"{{ pages["_meta"].meta.tagline ? ' class="tagline col-6"' }})>
                <a href="{{ "index"|link }}">
                    <h1>{{ site_title }}</h1>
                    {{ pages["_meta"].meta.tagline|markdown }}
                </a>
            </div>
            <!--Logo-->
            {% if pages["_meta"].meta.logo %}
                <div id="logo" aria-hidden="true" class="col-5 img">
                    <a href="{{ "index"|link }}">
                        <img src="{{ pages["_meta"].meta.logo|url }}" alt="" />
                    </a>
                </div>
            {% endif %}



            <!--Navigation-->
            <div id="nav" role="navigation" tabindex="-1">
                <ul class="nav nav-tabs nav-fill">
                    {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                        <li class="nav-item">
                            <a {% if page.id == current_page.id %} class="nav-link active" {% endif %} class="nav-link" href="{{ page.url }}">{{ page.title }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
    <!--Content-->
    <div id="main" role="main">
        <div class="container">
            {{ content }}
        </div>
    </div>
    <!--Footer-->
    <div id="footer" class="container-fluid bg-secondary bg-gradient text-white">
        <div class="row">
            <!--Footer text-->
            <div id="footer text" class="col">
                 <p>&copy Custom Theme made by <a href="https://electronicping.net">electronic Ping [eP]</a> <br />
				Diese Seite existiert nur für Rollenspielzwecke. Sie repräsentiert kein echtes Restaurant oder Bar!</p>
            </div>
            <!--Footer Social-->
            <div id="social" class="col">
                {% for social in pages["_meta"].meta.social %}
                    <a href="{{ social.url }}" title="{{ social.title }}" role="button">
                        <span class="icon-{{ social.icon }}" aria-hidden="true"></span>
                        <span class="sr-only">{{ social.title }}</span>
                    </a>
                {% endfor %}
            </div>
        </div>
    </div>

	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>

</body>
</html>
